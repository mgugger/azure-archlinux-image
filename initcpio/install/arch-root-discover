#!/bin/bash
# arch-root-discover — DEPRECATED
# All logic has been consolidated into the squashfs-overlay install hook
# and the systemd setup-root service. This file is kept as a no-op so
# mkinitcpio doesn't error if it's still referenced in HOOKS.

build() { :; }
help() { echo "Deprecated — see squashfs-overlay."; }
