#!/bin/bash
# arch-root-discover — DEPRECATED, systemd-only now
# All logic lives in initcpio/systemd/setup-root.
# This stub exists only because mkinitcpio requires a hooks/ file
# when add_runscript is used (it is no longer called).
run_hook() { :; }
