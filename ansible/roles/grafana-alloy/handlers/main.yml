---
- name: Reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart grafana alloy
  ansible.builtin.systemd:
    name: "{{ grafana_alloy_service_name_effective | default(grafana_alloy_service_name) }}"
    state: restarted
